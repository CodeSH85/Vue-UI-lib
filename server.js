
const Express = require('Express');

const app = new Express;

const port = 3006;

app.get('/api', (req, res)=>{
  const url = ''
})


app.listen(port,()=>{
  console.log(`server is running on ${port}`);
});
